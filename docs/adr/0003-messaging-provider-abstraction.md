# ADR 0003 — Messaging Provider Abstraction

**Status:** Accepted

Define a provider contract for messaging. Future implementations may include Evolution, WhatsApp Cloud, email and SMS. Domain code must not depend directly on a provider SDK/API.
