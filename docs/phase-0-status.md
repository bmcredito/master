# Phase 0 Status

Bootstrap implementation is contained in `phase/00-bootstrap`.

Phase 0 remains incomplete until its pull request is merged, Railway WEB and WORKER deployments are healthy, `prisma migrate deploy` succeeds, production `/health` and `/ready` return 200, and the harness passes with production infrastructure references. Do not start Phase 1 before those checks are recorded.

