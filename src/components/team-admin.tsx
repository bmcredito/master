"use client";
import { useState } from "react";
export function TeamAdmin({ teams }: { teams: Array<{ id: string; name: string; description: string | null; status: string; _count: { members: number } }> }) { const [error, setError] = useState(""); async function create(formData: FormData) { const response = await fetch("/api/teams", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(Object.fromEntries(formData)) }); if (response.ok) location.reload(); else setError((await response.json()).error); } return <><section className="card"><h2>Criar equipe</h2><form className="form" action={create}><input name="name" placeholder="Nome" required/><input name="description" placeholder="Descrição"/><button>Criar</button></form>{error && <p className="error">{error}</p>}</section><section className="card"><table><thead><tr><th>Nome</th><th>Status</th><th>Membros</th></tr></thead><tbody>{teams.map((team) => <tr key={team.id}><td>{team.name}</td><td>{team.status}</td><td>{team._count.members}</td></tr>)}</tbody></table></section></>; }
